name = "insulation-calculator-api"
main = "index.js"
compatibility_date = "2024-01-01"

# Workers configuration
workers_dev = true

# Routes (update with your domain after deployment)
# routes = [
#   { pattern = "api.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]

[observability]
enabled = true
